# Vault Instructions

You are inside the notes/ space. Repo-level CLAUDE.md routing rules apply.

- Cross-reference theme notes when writing stock notes and vice versa.
- Append to Updates Logs with date stamps. Never overwrite.
- Stock note filenames are `TICKER.md`. Trade filenames are `YYYY-MM-DD-TICKER-ACTION.md` (no parentheses).
- When adding a new ticker, also add it to `MOC - Stocks.md` and the relevant theme note.
